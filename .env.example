# ==============================================================================
# SECURITY: Gateway Authentication (REQUIRED for non-loopback bindings)
# ==============================================================================
# IMPORTANT: If you bind the gateway to a non-loopback address (e.g., 0.0.0.0),
# you MUST set a strong authentication token to prevent unauthorized access.
#
# Generate a strong token with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
#
# Minimum requirements:
#   - At least 32 characters
#   - Cryptographically random (use the command above)
#   - Do NOT use weak tokens like "password123" or "token"
#
# For local-only access (bind=loopback), authentication is optional but recommended.
# For network/internet access (bind=lan), authentication is MANDATORY.
#
CLAWDBOT_GATEWAY_TOKEN=

# Example (DO NOT USE THIS - generate your own):
# CLAWDBOT_GATEWAY_TOKEN=sOmE_StRoNg_RaNd0m_T0k3n_32pLuS_cHaRs

# ==============================================================================
# Optional: Twilio WhatsApp Integration
# ==============================================================================
# Copy to .env and fill with your Twilio credentials
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token_here
# Must be a WhatsApp-enabled Twilio number, prefixed with whatsapp:
TWILIO_WHATSAPP_FROM=whatsapp:+17343367101
